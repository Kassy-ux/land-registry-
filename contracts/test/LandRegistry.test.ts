import { expect } from 'chai'
import { ethers } from 'hardhat'

describe('LandRegistry', function () {
  it('should deploy successfully', async function () {
    // TODO Phase 3: write tests
    expect(true).to.equal(true)
  })
})
